// variables are declared with var.
var a; //declares a variable but its undefined
var b = 0;
console.log(b); //0
console.log(a); //undefined
console.log(a+b); //NaN
// NaN = not a number

//Use backtick
console.log(`${a} this is a string template`)